import 'package:flutter/material.dart';

class MlbTab extends StatelessWidget {
  const MlbTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('MLB'));
  }
}
