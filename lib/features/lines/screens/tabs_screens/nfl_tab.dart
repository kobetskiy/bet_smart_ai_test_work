import 'package:flutter/material.dart';

class NflTab extends StatelessWidget {
  const NflTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('NFL'));
  }
}
