import 'package:flutter/material.dart';

class NhlTab extends StatelessWidget {
  const NhlTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('NHL'));
  }
}
